# VendorGuard Agent 开发计划

> 生效：2026-09-08。替代原十天冲刺计划。
> 目标：完成一个自己能解释、修改和演示的供应商材料审查 Agent。

## 执行节奏

按可运行成果推进，不设置新的固定日历期限。一次只推进一个小功能，每个阶段同时保留实现时间和理解时间。需要提前准备的只有下一阶段直接使用的内容。

产品范围以 [PRD](VendorGuard-PRD.md) 为准；已有代码事实见 [项目上下文](../PROJECT_CONTEXT.md)。这份文件是唯一的当前进度看板，历史 Day 状态不再续写。

## 里程碑

| 阶段 | 可看到的成果 | 状态 |
| --- | --- | --- |
| M0 范围重定 | 当前 PRD、精简计划、统一接手入口，旧计划归档 | 已完成：2026-09-08，链接、归档和代码保留检查通过 |
| M1 Agent 最小循环 | 命令行真实模型调用校验工具，并依据结果回答或经 ask_user 追问 | 已完成：2026-09-10，两轮评审闭环；结论必须工具背书，真实追问经 ask_user 实证（见下） |
| M2 真实材料输入 | 从一种自制文本 PDF 提取事实，缺字段时追问并接受补充 | 已完成：2026-09-14；单测 247 通过，三类样例真实运行（含一次补充闭环），模型波动已记为已知限制（见下） |
| M3 制度检索与报告 | Agent 调用检索工具，输出有来源的结构化报告 | 已完成：2026-09-15（M3-0 至 M3-7）；8 题评测 3/3 + 2/2，真实运行三类样例齐备（见下） |
| M4 演示与讲解 | 单页工作台、完整案例、错误记录和可复现说明 | 已完成：2026-09-16（M4-0 至 M4-7）；六条真实验收记录 + README 与五分钟演示脚本（见下） |

### M1：先看到 Agent 工作

具体文件取舍、工具契约、实施顺序与第一条命令见 [M1 实施方案](VendorGuard-M1实施方案.md)。2026-09-10 M1 步骤 A-E 完成并通过一次外部评审闭环：`check_materials` 工具与 198 项单测/ruff/format/mypy 全绿，qwen3.7-flash 自动工具选择下正常/缺项/未知三案例入 `logs/agent-runs/`；E3 实验（纠正预算 1→2）验证机制后按规范回滚。

评审一轮修正记录：首轮真实“未知”案例经核查不忠实（模型把自己传参失败归因成用户材料问题，并声称“已触发人工审核流程”），当时“三案例均忠实”的结论过满，已撤回。修正后空回答/截断回答/迟到回答一律失败，被拒调用也入流水含原始参数；“缺失输入触发明确追问”当时以问号判据实证过一次（logs/agent-runs/20260910T111620458424.json，kind=question），该判据在二轮评审中被认定可绕过并撤销（见下）。

评审二轮修正记录：带问号的结论性文本可绕过工具背书，问号判据被撤销，追问改为显式工具 `ask_user`——纯文本一律不能作出口，追问经工具事件入档；`ask_user` 参数错误获得专属纠正桶（其余错误仍共享一次），纯文本纠正与用尽分支也带正文预览入事件。208 项单测/ruff/format/mypy 全绿。四场景复测：正常/缺件/未知通过，其中未知案例是“校验成功后经 ask_user 切题追问”的教科书链条；超范围交付率场景三次运行三种死因（空参数、`prompt` 别名、`null` 伪造），每次均为安全失败、伪造全被拦截，但未达成切题追问，记为已知限制：该模型在此场景行为高方差，随 M2 会话制（追问后接受补充并继续）消解。边界声明：`ask_user` 只能证明模型选择了追问，不能证明问题文本未夹带结论，忠实性仍靠提示词约束与案例验证，不宣称完全解决。

先确认现有模型服务支持 tool calling，使用对应官方 SDK；模型和凭证可用性在实施时核对，不在文档中假设已经配置。M1 接入业务工具 `check_materials` 与交互控制工具 `ask_user`（追问控制不计入 M1-M3 三个业务工具总数），内部复用规则纯函数，输入采用明确标注的结构化样例。

实现模型请求、工具参数校验、工具执行、结果回传和再次生成；加入 PRD 规定的轮数、工具次数与超时限制。业务规则工具只读或纯计算，不连接案件写入流程。

完成判据：一次真实模型运行有可查看的 tool call 参数、工具结果和最终回答；缺失输入能触发明确追问；使用模型响应替身验证未知工具、非法参数和循环上限。替身测试与真实模型演示分开记录。

理解检查：开发者能沿一次记录讲清“模型提出调用、程序执行工具、结果回到模型”，并修改一个工具参数或校验条件后运行验证。

### M2：接入一种真实材料

详细文件边界、工具契约、实施顺序与验收清单见 [M2 实施方案](VendorGuard-M2实施方案.md)。

选用成熟解析库读取一种自制文本 PDF，保留每页文本。增加 `read_material`；同一个 Agent 提取结构化事实，经 Pydantic 校验后调用 `check_materials`。扫描件明确提示不支持。

完成判据：完整和缺失字段两份样例均从实际 PDF 读取；来源页码能核对；缺失日期不会被编造；用户补充后可以在同一会话继续分析。用户文字补充标记为用户提供，不能伪装成 PDF 原文。

理解检查：能区分解析失败、模型提取错误和业务缺项，并独立定位其中一个案例。

2026-09-14 完成记录（M2-0 至 M2-5）：三份自制样例与生成脚本入库（样例 PDF 按二进制冻结）；`materials.py` 按页读取并给出可区分的失败码，`MaterialSources` 按定位符核对日期与清单文字；`agent_tools.py` 的提取事实管线只接收原始事实与来源，执照状态由程序按参考日期派生；`agent.py` 保留唯一循环，模型先 `read_material` 再 `check_materials`，未读材料不能校验、缺字段只能追问；`agent_session.py` 把用户补充登记为 `user_supplement@round:N` 并在同一会话跑第二轮。全量单测 247 通过，Ruff、格式检查与 mypy 全绿（当时 Docker 未启动，集成测试未跑，本轮改动不涉及数据库）。

真实模型运行记录（qwen3.7-flash，`logs/agent-runs/`）：完整样例 3 次全部通过（读取 → 校验 → 检查说明，来源页码正确，派生状态 valid）；扫描件样例在材料层被拒、退出码 2、未发模型请求；缺日期样例 3 次里 1 次走完“追问 → 登记补充 → 第二轮结论”，另两次为模型波动——一次先用掉唯一纠正机会（一次响应多个工具调用），随后用空字符串报日期被 Schema 拒绝而失败；一次补充后校验已通过但模型选择再次追问而未收尾。这两次都记为已知限制：边界按设计拒绝，属于模型行为方差，不是程序放行。

### M3：把依据接进工具循环

详细文件边界、数据与工具契约、检索方案、实施步骤与验收清单见 [M3 实施方案](VendorGuard-M3实施方案.md)。

按 [PRD 的 RAG 核心范围](VendorGuard-PRD.md#rag-核心范围) 分三步，每步跑通后再继续：

1. 只处理两份现行内部制度，复用章节与表格行解析，输出可核对原文的扁平片段；不接数据库入库。
2. 完成本地 embedding 缓存与单路 Top-5 检索，先从命令行查看返回原文，再包装为 `search_policy`。
3. 接入 Agent 报告与引用检查，运行 PRD 选定的 8 题，逐题记录正确依据、边界处理和错误引用；保留一次失败分析。

完成判据：结构化报告通过 Schema、已核对事实字段、实际规则结果和本次检索节点引用校验；自由文本是否受引用支持由固定案例人工核对。补充后重新检索；用旧制度和外部法规候选元数据验证版本清单过滤；所选 8 题按各自金标准完成验收，失败时定位并修复当前实现，不先扩充检索架构。记录语料、模型、题目、真实结果及原评测中未覆盖的 7 题。关键词与向量对比是具体漏检后的可选实验。

2026-09-15 方案修订：本期片段清单与 embedding 缓存只含两份现行制度的 22 条；全量 274 条是已有解析器的基线，负例版本与外部法规保留在仓库，用候选元数据混入测试证明排序前过滤有效，无需为负例生成 embedding。最终制度引用回指 `KnowledgeNode`，不引用召回用的 `chunk_key`；报告的结构化事实字段、规则 ID 与结果对应本次成功的 `check_materials`，第二轮补充后重新检索。BM25（纯 Python 字符二元组）和全量索引只在闭环后针对具体漏检做可选实验；embedding 模型实施时先探针复核可用性、维度与归一化。

2026-09-15 M3-1 完成记录：`retrieval/chunk_list.py` 按代码白名单 `CURRENT_EDITION_KEYS` 从冻结语料生成两份现行制度的 22 条片段（6 + 16），落盘 `data/retrieval/chunks_v1.jsonl`（22 行 / 27037 字节，sha256 `45b39ff0…`）。每条记录带 `node_key`、定位路径、归一化正文区间与快照/正文哈希；`char_start/char_end` 可在归一化正文里逐字切出 `display_text`，`source_path` 指向的冻结正文仍算得出 `normalized_sha256`。同源旧版 `demo_supplier_admission_policy_v1` 与 7 篇外部法规留在仓库作负例，不写入清单；清单缺失、为空、坏行或混入负例版本一律抛 `ChunkListError`，不返回空结果。命令行入口为 `scripts/build_chunk_list.py`（数据准备脚本，不进 Agent 循环）。新增 11 项单测，全量单测 258 通过，Ruff、格式检查与 mypy 全绿；本步不涉及数据库，集成测试未跑。

2026-09-15 M3-2 探针与聚焦测试：真实探针（`scripts/probe_embedding.py`，一次调用）确认 `qwen3.7-text-embedding` 可用、1024 维、返回向量已 L2 归一（范数 1.000000）、响应带 `index` 且与输入同序；单次输入上限 20 条，21 条被服务端 400 拒绝（`batch size is invalid, it should not be larger than 20`）。据此新增 `tests/unit/test_retrieval_embedding.py`（18 项，全部用替身、不打网络：分批 ≤20、按 `index` 回映射、维度/有限值/单位范数校验、缓存按"模型名 + 正文指纹"失效、查询向量不进缓存）与 `test_config.py` 的两项配置断言；依赖加入 `numpy`。临时落盘验证：22 条正文 2 次请求建成缓存（约 499 KB），二次运行 0 次新请求且向量逐分量一致，查询向量 1024 维、范数 1.000000；全量单测 278 通过，Ruff、格式检查与 mypy 全绿。开发者粘贴后已在仓库跑绿：`scripts/build_embedding_cache.py` 首次 2 次调用（20 + 2 分批）建成 `data/retrieval/cache/embedding_v1.json`（499719 字节，gitignore 不入库），二次 0 次调用复用；全量单测 278 通过。

2026-09-15 补一次实测（影响 M3-3 的可复现口径）：同一批正文两次独立构建的向量**不是逐分量相同**——22528 个分量里 7168 个有差异，最大 2.0e-4。因此"同一查询的 Top-5 可复现"只能绑定到**固定的缓存文件**，不能宣称模型输出稳定；缓存除了省调用，还承担"冻结向量"的作用。近似并列的分数跨重建可能换位，评测结论要写明用的是哪份缓存。

2026-09-15 M3-3 聚焦测试与验证：`retrieval/search.py` 做单路向量检索——先按现行白名单过滤候选，再算精确 cosine，分数降序、同分按 `chunk_key`，取 Top-K（缺省与上限均为 5）；查询参数问题（空、过长、`top_k` 越界）抛 `QueryError` 留给模型纠正，配置类失败（候选被过滤光、向量与片段数量不符、查询与缓存维度不符）抛 `SearchError` 直接中断而不用空结果冒充"没有依据"。新增 18 项单测全部用替身，关键是负例向量被造成与查询同向（余弦 1.0）：断言它们不返回、而分数更低的现行片段返回，证明过滤发生在打分之前。命令行验证 `scripts/preview_search.py`（真实缓存）：查询"关键物料品类是否可以先准入后补交质量证书"返回确定的 Top-5，含 `node_key`、定位、字符区间与原文，首位是"质量证书对关键物料品类属于必需材料，不再允许通过后再补交"；重复检索排序一致。全量单测 296 通过，Ruff、格式检查与 mypy 全绿。实现待粘贴后判绿。

M3-3 另一条实测限制：查询向量按设计每次现算，两次检索的**分数在第 4 位小数上会漂移**（实测 0.8236 / 0.8237）而排序不变。排序稳定依赖分差远大于该漂移，这再次说明分数不能当阈值；评测若出现 1e-3 量级的并列，要记成并列而不是断言单一名次。

2026-09-15 M3-4 测试与验证：`agent.py` 的循环接入 `search_policy`——工具声明 `query`（必填）与 `top_k`；新增启动装载 `load_policy_index()`，读片段清单并取回或构建向量缓存，清单缺失、缓存损坏或 embedding 不可用一律归一为启动错误（退出码 2）；成功的检索把本次真实返回的 `node_key` 与原文写进 `returned_nodes`（M3-5 的引用闸门只认这份集合），交给模型的工具结果含 `as_of`、`scope` 与每条节点的 `node_key`/定位/原文而**不含分数**；查询参数问题（含 `top_k` 越界）走共享纠正通道，检索配置或 embedding 不可用则直接失败且不再请求结论；工具预算与"一次响应只允许一个工具调用"沿用 M2。新增 9 项替身用例（含逐字段核对交给模型的 JSON、无索引时直接失败、embedding 失败时不继续请求）。全量单测 305 通过，Ruff、格式检查与 mypy 全绿；开发者粘贴后已在仓库跑绿，并用"把模型端点指向死端口"的方式实测 CLI 启动路径：打印 `[制度检索] 已装载 22 条现行制度片段` 后才进入循环，退出码 1（模型请求失败），未消耗真实模型调用。

M3-4 的一条设计取舍：检索**不要求先读过材料或先校验**。方案只钉住"未读材料不能 check"（M2 已有），检索是只读查询，提前查制度不构成越权；顺序仍由提示词引导（先校验事实、再取制度依据）。

2026-09-15 M3-5 完成记录：新增 `agent_report.py`（报告 Schema `ReviewReport`/`Finding`/`RuleResultClaim`、`parse_report_arguments`、引用闸门 `verify_report`、越权措辞黑名单）。闸门按方案 7 的顺序核对：报告声明的材料必须是本次那一份；`fact_fields` 必须属于本次核对通过的事实，`material_sources` 必须是本次核对过的定位且与所声明事实对应；`rule_results` 的规则 ID 与结果必须与本次 `evaluations` 逐字一致；`policy_citations` 必须是本次 `search_policy` 返回过的现行版本节点（chunk_key、旧版本节点、没检索过的节点一律拒绝）。Schema 层还挡住"零 findings 却声称审查完整"，并强制 `insufficient_evidence=true` 时必须写 `missing_reason`；明确缺依据的空 findings 报告是合法结论。`agent.py` 的结论出口改为**只有成功的 `submit_report`**：纯文本不再产生 `kind=answer`（改为纠正"必须调用 submit_report 交付"），报告通过校验后写进 `AgentRunOutcome.report` 与运行记录，CLI 逐条打印结论、事实、材料来源与制度引用。新增 23 项报告单测 + 5 项循环用例（报告先于校验被拒、纯文本不再收尾、伪造引用纠正后通过、两次不合格失败且不产出报告、运行记录带报告），并更新 M1/M2 时代以文本收尾的 14 处用例。全量单测 335 通过，Ruff、格式检查与 mypy 全绿。

2026-09-15 M3-5 真实运行与一处修正：完整材料样例（qwen3.7-flash，`--reference-date 2026-09-01`）第一次跑通到 `submit_report` 却**被措辞检查误杀**——模型写的是免责声明"规则未命中…但不等同于最终准入批准"，而黑名单只认"不等于"，把"不等同"当成宣告拦下，耗尽纠正后失败。已把"不等同/不表示/不意味着/不能说明"补进否定语表，并把这个真实句子固化成测试用例；补正后重跑同一案例通过（read → check → search → report，退出码 0，打印 2 条发现与 3 个现行节点引用）。**这条限制记在这里**：措辞检查是黑名单兜底，既可能误伤合法的免责表述，也可能漏掉变着说法的宣告；真正的保证是结构字段（事实、规则结果、节点引用）与固定案例的人工核对。

2026-09-15 M3-6 完成记录：一次补充之后第二轮被证明是**独立的一次运行**——`returned_nodes` 与 `checked_result` 都是 `run_review()` 的局部状态，CLI 第二轮重新调用它，开场只带材料与补充原文（`user_supplement@round:N`），不携带上一轮的检索节点或校验结论。新增 4 项循环用例（第二轮引用第一轮节点被拒、用自己检索到的节点通过、没重新校验就交报告被拒、开场不出现上一轮节点键）、3 项 CLI 用例（两轮闭环、第二轮确实重新调用 `run_review` 且共用同一会话、放弃补充时保持追问出口）。

同时修掉一个真实缺陷：CLI 的报告打印块原本只覆盖第一轮，**补充轮的报告只打印一行摘要、不打印结论与引用**；现在抽成 `_print_report()` 并由每轮的 `show()` 调用。又补一条结构性提醒：校验结果缺字段时，紧跟其后的 user 提醒会点名"必须调用 ask_user 追问，不能提交报告"——实测模型在缺字段时会反复去交报告而不是追问，靠拒绝调用纠正它已经太晚。全量单测 343 通过，Ruff、格式检查与 mypy 全绿。

2026-09-15 M3-6 真实运行：补充案例（`license_missing_date.pdf` + 输入补充日期）先用 `qwen3.7-flash` **连续 5 次全失败**，死因各异但都属模型行为（①空字符串当日期→纯文本收尾；②空字符串日期→改去交报告；③编造材料里没有的日期；④成功校验出缺字段后仍去交报告；⑤反复参数错误耗尽纠正）。**共同点：唯一那次共享纠正机会几乎每次都被一次格式/参数错误吃掉**，之后想纠正"该追问而不是交报告"已无额度。每次都是安全失败：没有产出报告、没有编造引用。

随后**换 `qwen3.8-max` 同案例一次通过**：`read → check(缺日期) → ask_user` 正常追问，登记补充后第二轮 `read → check(取补充来源) → search → submit_report`，退出码 0。运行记录 `20260915T145740004031.json` 显示第二轮检索返回 5 个节点，报告引用的 3 个节点全部来自这 5 个（`section_2`、`section_4`、`required_documents_policy_v3_section_4_table_row_1`），材料来源正是 `user_supplement@round:1` 与 `license_missing_date@page:2`——**"第二轮只用第二轮实际来源与引用"在真实运行里成立**。

结论与决定：**qwen3.7-flash 的 5/5 失败是模型行为方差，不是机制缺陷**，因此**不改 M1 评审过的纠正预算设计**（不给 `check_materials` 参数错误开专属桶）；M3-7 的评测与真实运行改用 `qwen3.8-max`，并在记录里写明模型名。留一条小gap给 M3-7：CLI 只在最后写一份运行记录，补充案例的第一轮事件（追问）不进档，只有第二轮的产出；要补的话按"每轮各写一份、各用自己的时间戳"处理。

理解检查：能解释分块、向量检索、引用检查分别做什么，并复现一个“检索相关但不足以支持结论”的失败案例。

### M4：形成可以独立演示的作品

详细边界、数据契约、HTTP 接口、实施顺序和验收清单见 [M4 实施方案](VendorGuard-M4实施方案.md)。

通过现有 FastAPI 接入一个审查工作台，展示材料、对话、工具执行结果和报告。接入现有认证；报告确认与旧审批流程分离。保存本地运行结果，支持重新查看报告和重新发起失败任务，不要求节点恢复。

完成判据：从页面完成正常审查、补充材料和无依据三个案例；一次真实工具或模型失败有清晰提示；检查桌面和窄屏页面的核心操作。README 写清环境、启动、样例、模型费用来源与实际限制。

理解检查：开发者能在约五分钟内演示主线，解释三个工具及一次失败处理，并独立完成一个小改动及相应验证。学习未完成时继续练习当前模块，不靠增加功能补偿。

2026-09-15 M4-1 完成记录（共用审查应用层）：新增 `src/vendorguard/review_application.py`——`ReviewCommand`（用户要求 + 参考日期 + 一份材料 + 已登记补充）、`ReviewRuntime`（客户端、规则、检索数据与模型名；用 frozen dataclass 持有活对象，测试可直接注入替身）、`build_review_runtime()`（装载顺序为模型配置 → 规则 → 客户端 → 检索数据，任何一项不可用都归一为 `AgentConfigError`，缺配置时一次报全所有变量名）与 `run_review_round()`（按命令新建会话、登记补充、调用现有 `run_review()`）。`agent.py` 的 `main()` 改接应用层并在函数内延迟导入它——`review_application` 在模块级导入本模块的 `run_review`，两处都在顶层导入会成环；CLI 不再自己建客户端或读片段清单，仓库仍只有一条 `run_review()` 工具循环。

两处有意改变，如实记录：①材料检查移到装载依赖之前，扫描件这类材料问题不再牵连检索装载（实测 `license_scanned.pdf` 退 2、未打印"已装载 22 条"、未发模型请求）；②第二轮不再是"同一会话再跑一次"，而是把用户补充追加进命令后由应用层新建会话——开场消息仍是 `user_supplement@round:1` 与补充原文，上一轮的检索节点与校验结论不会顺延。模型端点指向死端口实测启动路径：打印 `[制度检索] 已装载 22 条现行制度片段` 后 `[运行失败] 模型请求失败: Connection error.`，退出码 1，未消耗真实调用。

测试与验证：新增 `tests/unit/test_review_application.py`（8 项：answer/question/failed 三种返回、每命令新会话、缺配置一次报全、规则文件不可用、配置与检索装载的注入接线、应用层无数据库依赖），改写 `tests/unit/test_agent_cli.py`（注入点移到应用层；夹具把模型端点钉到本机死端口，防止任何漏替身的路径真的连上 `.env` 里的端点；新增"CLI 不自装载依赖"与"扫描件先于装载被拒"两项；原先"两轮共用同一会话"的断言改为"每轮各调一次 `run_review()` 且 session 不同对象"）。全量单测 353 通过（343 + 8 + 2），Ruff、格式检查与 mypy 全绿；本步不涉及数据库，集成测试未跑。

2026-09-15 M4-2 完成记录（本地审查记录与单轮持久化）：新增 `src/vendorguard/review_records.py`——`ReviewRecord` / `ReviewRoundRecord` / `ReviewFailure` / `ReviewFeedback` / `ReviewStatus` / `ReviewSummary` 与 `LocalReviewStore`（`create` / `get_for_owner` / `read_material_bytes` / `mutate_for_owner` / `list_for_owner`）。

契约与取舍：目录固定 `<root>/<review_id>/{material.pdf, record.json}`，`review_id` 由服务端生成且外部传入的值只按 UUID 解析——`../..` 这类片段一律按"记录不存在"处理，拼路径只用 UUID 的规范形式；上传文件名只作展示字段。`record.json` 带显式 `schema_version=1.0`，时间用 `AwareDatetime`（必须带时区）；写盘先写同目录临时文件再 `os.replace` 原子替换，替换失败时旧记录不变、临时文件清理。所有读写核对 `owner_user_id`，别人的记录、不存在的记录与非法 id 报同一种 `ReviewNotFoundError`，路由据此统一 404、不泄露存在性。同一 `review_id` 的"检查状态 + 写盘"都在 `mutate_for_owner()` 的同一次临界区内（按 id 的 `threading.Lock`）——原子替换只解决"半份文件"，串行化才解决"丢失更新"；本地演示固定单个 worker。状态机 `running` → `completed`/`question`/`failed`，`question` 只能补充一次，只有 `completed` 能提交一次反馈且不能覆盖；重跑由 `create(retry_of_review_id=...)` 建新记录（重跑资格判断留给 M4-4）。材料身份由材料层读出后登记一次，指纹必须与建档字节一致；工具事件在建轮次与写盘两处执行 `sk-` 脱敏，`agent.py` 的脱敏函数公开为 `redact_secret` 供记录层复用同一条规则（**不复制正则**）。`config.py` 新增 `review_data_dir`（默认 `var/reviews`），`.env.example` 与 `.gitignore` 同步；该目录要到 M4-3 才被 lifespan 使用。

测试与验证：新增 `tests/unit/test_review_records.py`（24 项，含 6 路 id 参数化：布局与逐字段读回、上传名只作展示、材料身份来自保存的 `material.pdf` 且只登记一次、非法 id 不落盘、别人的记录与不存在的 id 不可区分、answer 轮次带报告、失败落盘且完成记录不能改成失败、补充只允许一次、反馈不可覆盖、并发不丢更新、替换失败保旧记录、Schema 与时间校验、建档失败回滚、脱敏、列表摘要、重跑关联、跨实例读回、无数据库依赖）与 `test_config.py` 2 项。全量单测 379 通过，Ruff、格式检查与 mypy 全绿。两项额外验证：**去锁对照**——临时移除串行锁后并发用例立刻失败，证明它守的是真串行化；**真跨进程**——进程 1 建档并保存一轮 answer，进程 2（另一个解释器）读回轮次、报告、列表摘要与 2716 字节材料。本步不涉及数据库，集成测试未跑。

2026-09-16 M4-3 完成记录（受认证的发起、列表和详情接口）：新增 `src/vendorguard/review_routes.py`——`POST /api/reviews`（multipart，上限 10 MB、`request_text` 1..1000 字、`reference_date` 为 ISO 日期）、`GET /api/reviews?limit=1..50`、`GET /api/reviews/{id}`、`GET /api/reviews/{id}/material`；一轮 Agent 用 `run_in_threadpool()` 跑（方案 4.2），记录仓库的本地小文件读写直接调用。

契约与取舍：所有接口走现有 `get_current_user`，记录不存在与不属于当前用户统一 404；响应模型 `ReviewDetail`（完整记录 + `allowed_actions`）与 `ReviewListItem`（列表只给 7 个摘要字段）；`allowed_actions` 由记录算出、只存在于响应，不写进 `record.json`。错误体统一成 `{"error": {code, message, retryable}, request_id}`，本步的错误码是 `unauthenticated`(401)、`invalid_request`(400)、`upload_too_large`(413)、`review_not_found`(404)；**401 与请求校验两个处理器只对 `/api/reviews` 生效**，`/auth/login` 的 `detail` 形状保持不变（既有认证测试钉着它）。**201 只表示"记录已创建"**：扫描件这类材料错误也留下一条 failed 记录并随响应返回；缺字段、日期格式与超限发生在建档前，4xx 且不建档。材料失败码逐码映射（`material_scanned_unsupported` 等）且 `retryable=false`——重跑同一份字节没有意义；Agent 失败（模型/检索/预算）统一写 `agent_run_failed` 且 `retryable=true`，具体原因留在 `message` 里，**不解析文案去细分**。记录层为此补了三处能力：`with_round(..., failure=...)`（失败轮次必须同时给出 failure，非失败轮次不能带）、`available_actions()`（页面按它显示按钮）、`material_path_for_owner()`（先核对所有者再给出材料路径，路由不自己拼路径）。`app.py` 的 lifespan 在数据库之外装载 `review_store` 与 `review_runtime`（片段清单、向量缓存或模型配置不可用即启动失败），注册错误处理器并挂载路由；依赖新增 `python-multipart`。

测试与验证：新增 `tests/integration/conftest.py`——把启动时的审查依赖装载换成替身、记录目录指向临时目录，**否则每个 `create_app()` 的既有集成测试都会因缺模型配置与检索缓存而起不来**；新增 `tests/integration/test_review_routes.py`（16 项，全离线：认证用依赖覆盖、runtime/store 与实际那一轮用替身，覆盖 401 不建档、201 完整记录、扫描件 failed 且模型未被调用、超限在建档前拒、参数错误、跨用户 404、列表不泄露全文、材料字节一致、`allowed_actions` 随状态变化、`allowed_actions` 不落盘）；记录层新增 3 项。单测 382 通过，**完整集成测试 86 通过**（Docker 与数据库启动后首次全量跑，含既有认证与案件用例），Ruff、格式检查与 mypy 全绿。真实链路实跑：`demo.specialist` 登录 200、未认证 401、带 Token 发起审查 201（completed、`allowed_actions=["feedback"]`）、详情与 201 响应一致、材料字节一致、`demo.manager` 跨用户 404 且列表为空——**这一轮的 Agent 用替身，真实模型的 HTTP 运行留给 M4-7**。

2026-09-16 M4-4 完成记录（补充、反馈与重跑接口）：`review_routes.py` 增加到 707 行，**记录层一行未改**——M4-2 留下的 `with_supplement` / `with_feedback` / `available_actions` / `with_round(failure=...)` 刚好够用，本步只有路由层改动。

三个接口的契约：`POST /api/reviews/{id}/supplements`（只有 `question` 且未补充过能补充一次，其他状态 409 `state_conflict`；原文按原样保存、去空白后非空且 ≤2000 字；保存后记录回到 `running`，重新读材料、重新校验、重新检索，结果追加为第二轮，第一轮的追问与"无报告"状态保持不变）；`POST /api/reviews/{id}/feedback`（只有 `completed` 且未反馈，`decision` 只允许 `confirmed`/`recheck_requested`，备注 ≤1000 字；响应在记录视图上多一个 `business_state_changed=false`——反馈是报告层意见，不改业务状态也不调用任何准入或审批接口）；`POST /api/reviews/{id}/reruns`（201：复制本地保存的材料与原记录的请求/参考日期，创建新 `review_id` 并设 `retry_of_review_id`，旧记录不变）。

两处设计取舍：**重跑资格与页面按钮同源**——判定写成 `"rerun" not in source.available_actions()`，与详情响应里的 `allowed_actions` 用同一个函数，所以"页面能点的"和"接口放行的"不可能不一致，扫描件这类 `retryable=false` 的失败天然被挡住；**接口签名里没有任何模型、路径或状态参数**，材料从 `read_material_bytes(旧记录)` 复制，所以"重跑不能携带自选文件路径、模型名或业务状态"是结构性保证，不靠校验。路由层抽出 `_run_first_round` / `_execute_round` / `_load_material` 三个共用助手，使"发起"与"重跑"走同一条执行路径；`_mutate_or_conflict` 把 `ReviewStateError` 统一映射成 409 `state_conflict`（消息直接用记录层的原话），因为 `mutate` 在锁内抛错、写盘在其后，被拒的请求不会在磁盘上留下任何痕迹。

测试与验证：新增 11 项 HTTP 用例（补充保留两轮且第二轮命令带补充原文与同一份材料、补充只允许一次且被拒时不长新轮、补充原文 3 路长度校验、反馈只记一次且不改业务状态、非完成状态与非法 decision/超长备注、重跑新 id 与旧记录逐字段相等、非可重试失败与未要求重查的完成记录不能重跑、重跑忽略请求体里的 `model`/`status`/`material_path`/`request_text`/`reference_date`、三个写接口的所有者核对）。单测 382 通过、**完整集成测试 97 通过**（含数据库），Ruff、格式检查与 mypy 全绿。真实 HTTP 实跑两条链路（真实登录、Agent 那一轮用替身）：**追问→补充→报告**：`question`（动作 `supplement`）→ 补充 → `completed`、轮次 `[(1,'question'),(2,'answer')]`、动作变 `feedback`；**失败→重跑**：`failed` + `agent_run_failed` + `retryable=true`（动作 `rerun`）→ 反馈 `recheck_requested` 且 `business_state_changed=false`（动作 `rerun`）→ 重跑得到新 id 与 `retry_of_review_id`、旧记录仍为 failed 且轮次不变。**真实模型的 HTTP 运行仍留给 M4-7**。

2026-09-16 M4-5 完成记录（工作台基础页面）：新增 `src/vendorguard/workbench/`——`__init__.py` 提供 `GET /workbench`（公开页面壳）与 `GET /assets/workbench/{name}`（白名单静态资源：只服务 `styles.css` 与 `app.js`，未登记的名字一律 404，不按用户输入拼路径；两个响应都带 `Cache-Control: no-cache`），页面资源是 `index.html` / `styles.css` / `app.js`，原生 HTML/CSS/JS、不依赖 CDN。

前端约束的落点：所有来自接口的文本用 `textContent` 渲染（测试断言源码里没有 `.innerHTML` 赋值）；Token 只放 `sessionStorage`，收到 401 清 Token 并回到登录状态；一次操作只发一个请求，进行中立刻禁用按钮并显示"审查中…"；可执行动作只读服务端 `allowed_actions`，前端不推导状态；页面不写死任何示例记录。**样式口径（用户明确要求）**：Linear 风——深色平面、1px 细边框、圆角一律 4px（`--radius` 变量，测试断言所有 `border-radius` 都走它）、零渐变零投影、单一强调色；长文件名与错误消息 `overflow-wrap: anywhere`。布局：宽屏（≥1000px）主区域两栏（新建/状态/材料 与 轮次/报告），窄屏按 材料→状态→轮次→报告 堆叠，左侧历史栏固定宽度可滚动。

测试与验证：新增 `tests/integration/test_workbench.py` 12 项（页面壳公开且自包含、两个资源可取且内容类型正确、白名单 404（含 `../` 与编码穿越写法）、页面公开但数据接口仍 401、前端不拼 `.innerHTML`/Token 只在 sessionStorage/动作读 `allowed_actions`、样式平面（无渐变、无投影、圆角统一 4px）、不硬编码示例记录、资源目录内容固定）。浏览器实测（内置浏览器，登录与上传都是真实请求，只有 Agent 那一轮用替身）：未登录只见登录区；真实登录 200 后显示用户名与空历史；**从页面上传完整样例 → 报告**（6 项事实、1 轮、2 条发现、固定边界提示"初审报告不是准入决定"、"可执行动作: 确认报告或要求重查"）；刷新后从历史点开同一报告；提交中按钮立即禁用并显示"审查中…"，完成后恢复；宽屏两栏无横向溢出；360 px 单栏、无元素溢出、按钮不换行。**浏览器检查抓到并修掉两个真实缺陷**：① `[hidden]` 被 `.app { display: grid }` 压过，未登录也显示主区域；② 静态资源缺缓存控制，改了样式刷新还是旧样子。另按方案 8.1 把主区域从单列改成宽屏两栏。全量 491 通过，Ruff、格式检查与 mypy 全绿。限制：内置浏览器的真实键鼠命中不生效（改用页面内标准 API 驱动同一批处理器），窄屏截图拿不到（用程序化测量代替），**真实模型的页面全链路留给 M4-7**。

2026-09-16 M4-6 完成记录（来源、轨迹、补充、反馈与重跑交互）：先补上一处**保存证据**的缺口——`agent.py` 的 `search_policy` 工具事件增加 `nodes`（`node_key`/`edition_key`/`title`/`locator`/`text`，`chunk_key` 不进），这样页面展开制度引用时看到的就是**本次运行保存下来的原文**，不必也不允许在查看时重新检索（方案 4.5）；`tests/unit/test_agent.py` 相应加断言。

前端四类交互（全部只按服务端 `allowed_actions` 显示，前端不推导状态）：**工具轨迹**按发生顺序渲染每轮 `tool_events`（名称、成功/失败标记、参数与结果摘要；长内容用原生 `details` 折叠，出错的那一步自动 `open`）；**材料来源**把 `user_supplement@round:N` 直接显示成原文引用，把 `材料ID@page:N` 变成"查看材料 第 N 页"按钮（带 Token 取 PDF Blob，`window.open(blob#page=N)` 让浏览器阅读器停在那一页）；**制度引用**用 `details` 展开标题、定位路径、制度版本与本次保存的原文；**动作区**提供一次补充表单、确认报告/要求重查（显示"没有改动任何业务状态"）、用原记录重跑。

测试与验证：`tests/integration/test_workbench.py` 增至 16 项（新增交互锚点、**前端只调审查接口**（源码 `/api/` 路径集合恰为 `{"/api/reviews"}`）、用保存证据而非重新检索、`details` 折叠与出错展开、边界提示）。全量 495 通过（382 单测 + 113 集成），Ruff、格式检查与 mypy 全绿。浏览器实测四条路径：正常（2 条可展开引用含原文、来源按钮得到 `blob:…#page=1`、轨迹 4 步折叠、只显示反馈动作）；补充（追问→补充→两轮保留、补充来源显示原文）；无依据（0 发现 + 依据不足说明 + 边界提示）；失败（错误码 + 轨迹出错步骤自动展开 + 重跑新记录带 `retry_of_review_id`）。限制：真实键鼠命中不生效（用页面内标准 API 驱动同一批处理器）、截图通道不稳定（拿到一张桌面图）、**真实模型的页面全链路留给 M4-7**。

2026-09-16 M4-7 完成记录（真实验收与作品收尾，M4 收尾）：用真实模型（`qwen3.8-max`）**从页面**跑完整材料、缺有效期补充、无依据请求三条路径，外加扫描件（材料层拒绝、不发模型请求）与一次真实失败 + 重跑。六条记录都在 `var/reviews/`（gitignore）：完整材料 `completed` 1 轮 4 次模型请求 2 条发现；缺有效期 `completed` **2 轮**（追问 3 次 + 报告 4 次，第二轮重新读取/校验/检索，补充来源显示原文、材料来源定位第 2 页）；无依据请求 `completed`（模型检索后写明缺少支持该结论的制度依据）；扫描件 `failed` 0 轮（`material_scanned_unsupported`，不提供重跑）；模型端点不可达 `failed` 1 轮（`agent_run_failed`，1 次模型请求 / 0 次工具调用，可重跑）——**如实说明**：这次失败是用不可达端点制造的基础设施失败，不是模型行为失败；对它点「用原记录重跑」后新记录 `completed` 并带 `retry_of_review_id`，旧失败记录一字未改。

布局与浏览器检查：桌面 1280 px 主区域两栏（421 + 442）无横向溢出；360 px 单栏、无页面溢出、按钮不换行；唯一 `scrollWidth > clientWidth` 的元素是顶栏用户名——它按设计用 `text-overflow: ellipsis` 截断。交互期 `error`/`unhandledrejection` 收集器为空（无 JS 错误）。限制如实记录：内置浏览器的真实键鼠命中不生效（改用页面内 `requestSubmit()` 等标准 API 驱动同一批处理器与真实请求），截图通道在会话中途失效（M4-5/M4-6 已拿到同界面桌面截图，本轮窄屏改用程序化测量）。

作品收尾：`README.md` 按 M4 现状重写（环境准备、模型费用来源与缓存说明、启动两条命令、工作台入口与演示账号、三份样例用途、六条真实验收记录、五条实际限制、测试与检查命令、文档导航）；新增[五分钟演示脚本](VendorGuard-五分钟演示.md)（从启动与登录到正常/补充/无依据/失败重跑四条路径的逐步预期结果、三条边界讲解词与常见追问）。全量 495 通过（382 单测 + 113 集成），Ruff、格式检查与 mypy 全绿。

## 控制范围

任何新增组件先对应一个当前案例中的具体问题。默认范围只有一个 Agent、三个业务工具、一种材料和一个工作台；需要增加时先说明哪个现有案例无法完成。

每次功能改动运行相关测试和必要的 lint、类型检查。只有修改数据库时才做迁移验证；纯文档改动检查链接与一致性即可。真实模型结果具有波动，记录实测情况，不把一次成功当作质量保证。

M0–M4 到此收尾：里程碑表里全部为已完成，后续工作由 [PRD](VendorGuard-PRD.md) 与求职需要另行决定（不再有「当前唯一小功能」这一行）。M3 的 8 题评测只覆盖 PRD 选定的 8 题，其余 7 题（历史日期与外部法规）明确未覆盖；关键词与向量对比只在出现具体漏检时才做，目前 8 题无漏检。

2026-09-15 M3-7 完成记录（M3 收尾）：评测脚本 `scripts/run_rag_eval.py` 只读 `data/evals/rag_phase1.json` 里 PRD 指定的 8 个 `case_id`，结果写入 `data/evals/rag_phase1_m3_8q_result.json`（原数据未改）。逐题结果：3 道正常题的必需依据**全部进入 Top-5（3/3）**；2 道需引用现行制度纠正前提的边界题（"先准入后补交"、"自愿认证是否意味着不必提交"）**必需依据也都在 Top-5（2/2）**；3 道 `no_answer` 类题按设计无法由检索判定（检索不设阈值、恒返回 Top-5），标为待人工核对并逐题列出 Top-5 与"现行版本但不应作为依据"的节点。**非现行被禁版本命中 0**（白名单在打分前过滤），程序可判定的错误或无效引用 0。评测暴露一个语义细节并已写进脚本注释：`forbidden_edition_keys` 对旧版/外部法规是"不该被检索到"，对现行版本是"结论不能以它为依据"，两者不能混算。

真实运行记录：完整材料样例（`qwen3.7-flash`，退出码 0，2 条发现 + 3 个现行节点引用）、补充材料样例（`qwen3.8-max`，两轮闭环，退出码 0，引用全部来自第二轮检索的 5 个节点）、扫描件样例（材料层拒绝，退出码 2，未发模型请求）。三类样例的记录都在 `logs/agent-runs/`（gitignore，不入公开仓库）。

M3 已知限制（如实记录，不当作已解决）：①`qwen3.7-flash` 在补充案例上 5 次全失败、换 `qwen3.8-max` 一次通过，属模型行为方差；②措辞检查是黑名单兜底，可能误伤免责表述也可能漏掉改头换面的宣告；③服务端 embedding 不是逐分量确定的，"同一查询可复现"绑定的是具体缓存文件（结果 JSON 里记了 `cache_key`）；④CLI 只在最后写一份运行记录，补充案例第一轮的追问事件不进档。
