# VendorGuard 当前项目事实

> 更新：2026-09-20。本文只维护当前实现、验证证据和已知边界，不再记录逐阶段施工历史。
> 详细开发过程仍可从 Git 历史恢复。

## 1. 项目定位

VendorGuard 是面向供应商准入场景的材料审查 Agent，也是 AI Agent / AI 应用开发求职项目。
输入是一份文本型供应商 PDF、审查要求和参考日期；输出是带材料来源、规则结果和现行制度引用的
结构化初审报告，或者一次明确的补充问题、可解释失败。

报告不是供应商准入决定。确认报告、要求重查和关联重跑都不会修改案件状态、供应商状态或合格
供应商清单。产品范围以 [PRD](project_docs/VendorGuard-PRD.md) 为准；案件兼容状态见
[状态数据字典](project_docs/VendorGuard-状态数据字典.md)。

## 2. 当前交付

M0-M4 已收尾，当前没有尚未完成的里程碑：

- 命令行入口和受认证的单页工作台复用同一套审查应用层与同一个 `run_review()` 工具循环。
- 工作台支持登录、新建审查、历史列表、材料回看、工具轨迹、制度引用展开、一次补充、报告反馈和
  失败重跑。
- 页面动作来自服务端计算的 `allowed_actions`；前端不复制状态转换规则。
- 审查记录保存在本地 `var/reviews/<review_id>/`，每条记录包含材料、轮次、报告、失败、反馈和
  `retry_of_review_id`。这是演示证据，不是业务案件。
- HTTP 数据按 `owner_user_id` 隔离；不存在、非法 ID 和跨用户访问统一返回 404。

完整现场流程见[五分钟演示](project_docs/VendorGuard-五分钟演示.md)。

## 3. Agent 与程序边界

仓库只有一条 Agent 工具循环：

```text
read_material -> check_materials -> search_policy -> submit_report
       \-> 缺信息时 ask_user -> 用户补充 -> 新一轮完整执行
```

- 模型负责选择工具、提出补充问题和组织报告解释。
- Python 负责材料身份、来源核对、日期有效性、规则计算、版本过滤、引用放行、预算、状态和重试。
- `ask_user` 与 `submit_report` 是唯一追问和结论出口；模型纯文本不能结束审查。
- `check_materials` 只接受 `read_material` 已登记的材料事实；日期状态由程序按参考日期派生。
- `submit_report` 核对材料来源、事实值、规则结果和引用节点；引用必须来自本次检索实际返回集合。
- 越权宣告“准入批准”等措辞会被拒绝并要求改写，但措辞黑名单只是兜底，不宣称覆盖所有语义变体。

## 4. 材料与制度检索

### 材料

- `materials.py` 使用 `pypdf` 逐页提取文本，保留页码、材料 ID 和 SHA-256。
- 文本为空但页面存在时识别为扫描件并在材料层拒绝，不调用模型。
- 一次补充保留原文，并与 PDF 材料来源分开显示；第二轮重新读取、校验和检索。

### 制度

- 当前语料是两份版本化内部教学制度，共 22 个章节或表格行片段。
- `CURRENT_EDITION_KEYS` 在打分前过滤非现行版本，旧版和外部法规仅作负例资产。
- 正文向量按 20 条分批生成并缓存；缓存以模型名和正文指纹整体失效，查询向量不缓存。
- 检索使用精确余弦 Top-5，同分按 `chunk_key` 排序；返回节点带版本、定位路径和原文。
- 报告引用只能指向当前运行实际返回的 `node_key`，页面展示运行时保存的原文，不重新检索。

外部法规来源的核验记录见
[RAG 一期官方语料来源核验](project_docs/VendorGuard-RAG一期官方语料来源核验.md)。

## 5. 主要模块

| 模块 | 当前职责 |
| --- | --- |
| `agent.py` | 唯一工具循环、工具 Schema、预算、纠正、运行记录 |
| `agent_report.py` | 结构化报告、材料来源与引用闸门 |
| `materials.py` | PDF 读取、逐页文本、扫描件识别与材料指纹 |
| `rules.py` / `policy.py` | 确定性规则计算和 YAML 规则装载 |
| `retrieval/` | 片段清单、Embedding 缓存、版本过滤和 Top-K 检索 |
| `review_application.py` | CLI 与 HTTP 共用的审查命令和运行依赖 |
| `review_records.py` | 本地记录 Schema、原子写、所有权和状态机 |
| `review_routes.py` | 认证 HTTP 接口、输入限制、错误映射和线程池执行 |
| `workbench/` | 无构建链的单页工作台 |
| `security.py` | Argon2 密码、JWT、认证和演示账号种子 |

数据库中的认证、用户、案件和审计资产继续保留；当前工作台审查记录没有接入案件审批或 AVL。

## 6. 当前验证证据

2026-09-20 现场检查：

- `384` 项单元测试通过；
- Ruff 检查通过；
- Ruff 格式检查通过；
- mypy strict 通过。

2026-09-16 M4 收尾历史验证：

- `113` 项集成测试通过；
- `qwen3.8-max` 从页面完成完整材料、缺项补充、依据不足、扫描件拒绝、端点失败和关联重跑六条
  真实路径；
- 桌面 1280 px 与窄屏 360 px 布局检查无页面横向溢出，交互期无 JavaScript 错误。

当前机器在 2026-09-20 未能连接 Docker，因此 `113` 项集成测试没有在本次文档整理中重跑；该数字
必须继续表述为 M4 收尾历史结果。

制度检索评测只选取冻结数据集中的 8 题：3 道正常题和 2 道纠正前提题的必需依据进入 Top-5，
非现行禁用版本命中 0；3 道 `no_answer` 题仍需人工核对语义支持关系，不能宣称“8/8 全自动正确”。

## 7. 已知边界

- 只支持文本型 PDF，不做 OCR 或多文件材料包。
- 只允许一次补充，不提供长期多轮对话。
- Embedding 服务端输出存在微小数值波动，Top-5 可复现性绑定冻结缓存。
- 引用闸门验证节点真实、版本有效且来自本次检索；自由文本是否被引用语义充分支持仍需固定案例评测。
- 本地记录固定单进程、单 worker，不承诺共享存储、多租户、对象存储或生产 SLA。
- 一轮 Agent 同步等待并在线程池执行；没有后台队列、SSE、WebSocket 或节点级恢复。
- 项目不自动批准供应商、不更新业务案件，也不自动写入 AVL。

## 8. 继续工作时的规则

当前没有默认的下一里程碑。开始新工作前，先根据求职反馈或评测缺口定义一个可运行的小功能及完成
标准，再修改代码。不要因为历史规划重新引入 Multi-Agent、LangGraph、复杂检索基础设施或生产级
平台能力。

涉及案件状态、规则和数据库迁移时，先读取状态数据字典、现行规则、迁移和相关测试。完成改动后，
运行与风险相称的测试，并同步本文发生变化的事实。

常用检查：

```powershell
uv run --no-sync pytest -q -p no:cacheprovider tests/unit
docker compose up -d database
uv run --no-sync pytest -q -p no:cacheprovider tests/integration
uv run --no-sync ruff check src tests
uv run --no-sync ruff format --check src tests
uv run --no-sync mypy
```
